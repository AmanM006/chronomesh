version: '3.8'

services:
  chronomesh:
    build: .
    container_name: chronomesh-mission-control
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped
